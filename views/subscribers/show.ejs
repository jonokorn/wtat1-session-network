<h1>User Data for <%= subscriber.name %></h1>

<table class="table">
    <tr>
        <th>Name</th>
        <td><%= subscriber.name %></td>
    </tr>
    <tr>
        <th>Email</th>
        <td><%= subscriber.email %></td>
    </tr>
        <tr>
        <th>Zip Code</th>
        <td><%= subscriber.zipCode %></td>
</table>


 <div>
    <a href="/subscribers">View all users</a>
   </div>
   <div>
    <a href="<%=`/subscribers/${subscriber._id}/edit`%>">
        Edit User Details
 </a>
</div>
<div>
 <a href="<%= `/subscribers/${subscriber._id}/delete?_method=DELETE` %>"
 onclick="return confirm('Are you sure you want to delete this record?')">Delete</a>
</div>

<p>This subscriber has  been interested in events <%= subscriber.events.length %></p>